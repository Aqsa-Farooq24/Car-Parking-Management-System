# Car-Parking-Management-System
Car Parking Management System (CLI, C, Linux) is a command-line application for Ubuntu that manages vehicle entry/exit, parking slot allocation, billing, and record keeping using file-based storage. It demonstrates OS concepts like FCFS scheduling, process management (fork/exec/wait), and system calls.
